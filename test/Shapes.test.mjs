import { expect } from "chai";
import { TShape, IShape, SingleShape } from "../src/Shape.mjs"
/*
describe("Rotating shapes", () => {
    let shape;
    beforeEach(() => {
      shape = new TShape('T', 0);
    });
  
    it("initial TShape", () => {
        expect(shape.toString()).to.equalShape(
          `.T.
           TTT
           ...`
        );
    });
    it("initial TShape", () => {
        shape.rotateLeft()
    expect(shape.toString()).to.equalShape(
        `.T.
         TT.
         .T.`
    );
    });
    it("initial TShape", () => {
        shape.rotateRight()
    expect(shape.toString()).to.equalShape(
        `.T.
         .TT
         .T.`
    );
    });
});
*/